<section class="card">
    {{log blogs}}
  <a href="/gallery/{{id}}">
    <header>{{name}}</header>
    <img src="/images/{{blogs.[0].filename}}" alt="{{paintings.[0].description}}">
  </a>
</section>